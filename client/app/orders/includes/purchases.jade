table.table
  thead
    tr
      th Date
      th Total
  tbody
    tr(ng-repeat='purchase in oc.purchases.receipts | orderBy:"-created_at"')
      td {{purchase.created_at | amDateFormat:'LLL'}}
      td {{-purchase.total | currency }}